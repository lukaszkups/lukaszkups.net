{
  "name": "lukaszkups.net",
  "version": "1.0.0",
  "description": "website of lukaszkups",
  "main": "index.js",
  "scripts": {
    "server": "node -e 'require(\"./index.js\").init()'",
    "deploy": "node -e 'require(\"./index.js\").deploy()'",
    "assets": "node -e 'require(\"./index.js\").assets()'",
    "compile": "node -e 'require(\"./index.js\").once()'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "lukaszkups",
  "license": "UNLICENSED",
  "devDependencies": {
    "fs": "0.0.1-security",
    "ftp-deploy": "^2.3.0"
  },
  "dependencies": {
    "fs-extra": "^7.0.1",
    "http-server": "^0.11.1",
    "node-sass": "^4.11.0",
    "pug": "^2.0.3",
    "showdown": "^1.9.0",
    "uglify-es": "^3.3.9"
  }
}
