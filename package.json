{
  "name": "lukaszkups.net",
  "version": "1.0.0",
  "description": "website of lukaszkups",
  "main": "index.js",
  "scripts": {
    "server-linux": "nvm exec 13 node -e 'require(\"./index.js\").server()'",
    "server": "node -e 'require(\"./index.js\").server()'",
    "compile": "node -e 'require(\"./index.js\").compile()'",
    "deploy": "node -e 'require(\"./index.js\").deploy()'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "lukaszkups",
  "license": "UNLICENSED",
  "devDependencies": {
    "fs": "0.0.1-security",
    "ftp-deploy": "^2.3.0"
  },
  "dependencies": {
    "fs-extra": "^7.0.1",
    "ftp-deploy": "^2.3.1",
    "http-server": "^0.12.1",
    "node-sass": "^4.13.1",
    "node-watch": "^0.6.0",
    "pug": "^2.0.3",
    "showdown": "^1.9.1",
    "uglify-es": "^3.3.9"
  }
}
